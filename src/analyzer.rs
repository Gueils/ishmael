/// Ishmael::Analyzer
/// Allows to call whales to analyze current repository in /repos directory.
///
/// Usage:
/// ```
/// ```
pub struct Analyzer {}

impl Analyzer {
    pub fn new() -> Analyzer {
        Analyzer {}
    }

    pub fn process(&self) {}
}
